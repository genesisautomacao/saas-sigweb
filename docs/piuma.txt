================================================================================
MÓDULO MOBILIDADE URBANA — PIÚMA/ES
Plano de implementação a partir do levantamento de campo (Líder Engenharia)
Criado em 2026-09-01 · Revisado em 2026-09-01 (v2 — decisões do usuário aplicadas)
Fonte: D:\Backup Dell Antigo\Andamento\Lider Engenharia\Sigweb - Piúma\
  json_concluido_30-08-26\ (20 GeoJSONs, ~2.750 features, CRS84 ✓)
Este arquivo é o BACKLOG do módulo — atualizar status (⏳/🔄/✅) ao concluir.
================================================================================

--------------------------------------------------------------------------------
1. INVENTÁRIO ANALISADO (o que veio no pacote)
--------------------------------------------------------------------------------

POLÍGONOS
  bairros.json ................ 17 MultiPolygon  name/id/area_geo — JÁ EXISTE no
                                 sistema (camada Bairro do imobiliário): importa
                                 pela ação "Importar Mapa (GIS)" sem código novo.
  zonas.json .................. 9  MultiPolygon  zonas do estudo Origem/Destino
                                 (name + origens % + destinos %).
  poligonal.json .............. 6  MultiPolygon  quadrantes de estudo (A..E).
  polo_industrial.json ........ 1  MultiPolygon  "Zona de Interesse Empresarial".
  setores.json ................ 86 MultiPolygon  setores censitários IBGE
                                 (codigo 15 dígitos + situacao Urbana/Rural).

LINHAS
  trechos.json ................ 612 MultiLineString — A JOIA DO LEVANTAMENTO:
                                 26 atributos tabulares por trecho de via
                                 (pavimentação, calçadas E/D, acessibilidade,
                                 estacionamento, vegetação, tipologia, largura).
  rodovias.json ............... 896 MultiLineString — malha DER-ES do ESTADO
                                 INTEIRO. DECIDIDO: importar SÓ o que intersecta
                                 Piúma (gestora da mobilidade confirmou).
  fluxos.json ................. 63 MultiLineString — linhas de desejo do estudo
                                 O/D (ver explicação em §6.4).
  rotas_transporte_cargas.json  34 MultiLineString — ruas de rota de carga.
  eixos_comerciais.json ....... 7  MultiLineString — eixos comerciais.
  rede_cicloviaria.json ....... 5  MultiLineString — ciclovia/ciclofaixa.
                                 DECIDIDO (6.7): nome importa como está, sem
                                 extrair situação.

PONTOS
  sinalizacao.json ............ 692 Point — sinalização viária (tipo Vertical/
                                 Horizontal + name livre de coleta por voz).
  sinalizacao_estacionamento .. 140 Point — sinalização de estacionamento
                                 (12 categorias limpas).
  comercio_e_servicos.json .... 58 Point — POIs comércio/serviços.
  religioso.json .............. 43 Point — templos.
  turismo_lazer_esporte.json .. 30 Point — quadras, campos, ginásios.
  educacao.json ............... 21 Point — escolas/creches/faculdades.
  saude.json .................. 11 Point — hospital + UBSs.
  pontos_transporte_cargas .... 11 Point — postos de combustível.
  industrias.json ............. 10 Point — indústrias.

--------------------------------------------------------------------------------
2. MODELAGEM (v2) — 6 entidades + 1 CATÁLOGO cobrem os 19 layers
--------------------------------------------------------------------------------
Racional: não criar 19 tabelas. Agrupar por FORMA + COMPORTAMENTO e diferenciar
por campo `tipo`/`categoria`. No mapa cada camada do levantamento tem seu
PRÓPRIO toggle (padrão "um checkbox por fluxo" da camada Processos).

Todas as entidades geográficas: BelongsToTenant + HasTenantSequentialId +
SoftDeletes, coluna geo (PostGIS) + accessor geo_json/setGeoAttribute,
`dados_customizados` JSONB + GIN, LogsActivity + LogsGeometryChanges,
area_geo/extensao_geo cacheada em METROS (padrão do sistema; exibição pode
formatar em km — registrar no RecalcularGeoMetadata::ENTIDADES).

  2.1 MobTipoSinalizacao (mob_tipos_sinalizacao) — CATÁLOGO, sem geo ... ~35 tipos
      DECISÃO 6.1: a placa NÃO carrega texto livre — ela APONTA para um tipo
      pré-cadastrado. Colunas: name ("Pare", "Proibido Estacionar", "Faixa de
      Pedestres"...), tipo (vertical|horizontal), cor (ColorPicker), icone
      (upload png/jpg — molde CategoriaChamado), codigo_ctb nullable (R-6a,
      A-12...), ativo, ordem. BelongsToTenant (cada município gere seu catálogo);
      SEED inicial com os tipos identificados no levantamento de Piúma.
      → O que varia entre duas placas "Pare" é SÓ a posição no mapa.

  2.2 MobSinalizacao (mob_sinalizacoes) — POINT ........................ 832 feats
      tipo_sinalizacao_id FK (catálogo acima), descricao_original (o texto cru
      da coleta, preservado p/ auditoria/conferência), observacao.
      Absorve sinalizacao.json + sinalizacao_estacionamento.json.
      Importação: de/para por palavra-chave name→tipo do catálogo (classifica
      >90%; não classificado cai no tipo "A Classificar" p/ revisão na Onda 4).
      Mapa: cor e ícone vêm do CATÁLOGO; toggles sinalização × estacionamento.

  2.3 MobPontoInteresse (mob_pontos_interesse) — POINT ................. 184 feats
      categoria (comercio_servicos | educacao | saude | religioso |
      turismo_lazer_esporte | industria | posto_combustivel), name, numero.
      Absorve os 7 JSONs de POI. Um toggle por categoria no acordeon.

  2.4 MobTrecho (mob_trechos) — MULTILINESTRING ........................ 612 feats
      SEM name (decisão 6.3: trecho urbano não tem nome — referência é o
      sequential_id, que herda o `id` do JSON na importação, padrão do sistema).
      Colunas estruturais: tipologia_da_via, tipo_de_pavimentacao,
      estado_conservacao_pavimentacao, classe_faixa_rodagem,
      dimensionamento_da_via, extensao_geo, logradouro_id FK nullable (futuro),
      SENTIDO + AZIMUTE (ver §3).
      Os outros ~20 atributos (calçadas E/D, acessibilidade, estacionamento ×7,
      vegetação ×3) → dados_customizados, semeados como campos customizados de
      um "kit Piúma" (padrão KitCamposCustomizadosService) — rótulo/lista
      white-label e entrada de graça em import/export/modais.

  2.5 MobEixo (mob_eixos) — MULTILINESTRING ............................ ~60 feats
      tipo (ciclovia | eixo_comercial | rota_carga | rodovia), name (COMO VEIO —
      decisão 6.7), extensao_geo em METROS (exibição em km onde couber — 6.2).
      Extras da rodovia DER-ES (sre, sigla, sit, km_*...) → dados_customizados.
      Absorve rede_cicloviaria + eixos_comerciais + rotas_transporte_cargas +
      rodovias RECORTADAS (ST_Intersects com o município — decisão P3).

  2.6 MobZona (mob_zonas) — MULTIPOLYGON ............................... 102 feats
      tipo (zona_od | quadrante | polo_industrial | setor_censitario), name,
      codigo (setor IBGE), situacao (Urbana/Rural), origens/destinos decimal
      nullable (% do estudo O/D), area_geo (recalculada via PostGIS — 6.6).
      Absorve zonas + poligonal + polo_industrial + setores.

  2.7 MobFluxo (mob_fluxos) — MULTILINESTRING .......... 63 feats · ⏳ AGUARDA P4
      Linha de desejo O/D: destino (centro|norte|sul|leste|nordeste|sudoeste),
      valores int (volume). Camada analítica (espessura ∝ valores no engine).
      Importar SÓ geometry + fluxo + valores (point_x/y, centro_x/y e pares
      cardinais zerados = artefato da ferramenta — descartar).
      ⚠️ Entra apenas se o usuário confirmar após a explicação de §6.4;
      Sudoeste tem 18 linhas (demais têm 9) — conferir duplicação com o geo.

--------------------------------------------------------------------------------
3. TRECHOS — SENTIDO, DIREÇÃO E AZIMUTE (decisões 6.3 + P2)
--------------------------------------------------------------------------------
FATO: no material de origem a direção era só VISUAL (setas no desenho); o JSON
não tem campo tabular de sentido. Modelo aprovado:

  a) `sentido` enum: mao_unica | mao_dupla | NULL (= não classificado).
  b) DIREÇÃO = ORDEM DOS VÉRTICES da linha (ponto inicial → final). APROVADO
     pelo usuário: ao criar um trecho novo, a direção nasce do desenho (de onde
     começou para onde terminou). Nada de azimute digitado à mão.
  c) `azimute` decimal — CALCULADO automaticamente (read-only): bearing do
     primeiro ao último vértice via ST_Azimuth, em graus 0–360, recalculado
     sempre que a geometria muda (mesmo gatilho do extensao_geo). Serve para
     relatórios e para a futura inferência a partir das setas do projeto (P2).
  d) SETAS NO MAPA: SIM — style function no engine desenha setas rotacionadas
     pelo bearing nos midpoints dos segmentos quando sentido=mao_unica (dupla:
     seta nos dois sentidos ou nenhuma — refinar no visual). Ligam/desligam
     junto com a camada.
  e) Ferramentas: botão "Inverter sentido" (ST_Reverse) no modal/ficha +
     select do sentido no "Editar Dados". Preview com seta na criação.
  f) BACKFILL dos 612: importam com sentido=NULL + modo de classificação em
     massa no mapa (filtro "sem sentido", clique → mão única/dupla/inverter,
     contador de progresso). P2 (exportar as setas visuais do projeto p/
     inferir automático) fica para depois — usuário vai verificar com o geo.

--------------------------------------------------------------------------------
4. O QUE JÁ EXISTE E SERÁ REAPROVEITADO
--------------------------------------------------------------------------------
  · Gate por módulo: HasTenantModule + $tenantModule = 'mob_infra' (chave já
    existe no select do TenantResource). DECISÃO P1: Piúma ativa também o
    módulo `imobiliario` (bairros e cadastro base) — zero código.
  · Importação: ImportadorGisService (modos adicionar/atualizar/substituir,
    sequential_id = id do JSON, dados_customizados por slug) — registrar as
    entidades mob_* no seletor + ENTIDADE_CUSTOM.
  · Campos customizados + CampoDominio: estender CampoCustomizado::ENTIDADES
    com mob_trecho (e mob_eixo p/ extras de rodovia) + kit Piúma.
  · Permissões: PermissionsSeeder + Policy por model (sem Policy o Filament
    LIBERA) + CAIXA "Mobilidade Urbana" na RoleResource + whitelist
    EditRole/CreateRole + gates ver_camada_mob_* no painel de camadas.
    ⚠️ Deploy: seeder + permission:cache-reset (incidente 2026-08-05;
    Policies novas usam temPermissao(), nunca hasPermissionTo).
  · Mapa: case por camada no MapDataController (ST_AsGeoJSON na query, nunca
    accessor em loop) + layerConfigs/fetchAndDrawLayer + acordeon próprio +
    traits Has*Actions (criar/editar geometria pela Mesa de Desenho, editar
    dados, soft delete) — paridade total com quadra/logradouro.
  · Exports: padrão ExportService → Excel + PDF (mínimo pedido) + CSV + XML
    de brinde, 4 botões no ActionGroup de cada List page.
  · Auditoria: LogsActivity (logAll+logExcept geo) + LogsGeometryChanges.
  · Unidades (decisão 6.2): banco sempre em METROS (padrão do sistema);
    rodovias/eixos EXIBEM km formatado (state/1000) nas tabelas, fichas e
    relatórios. Não é preciso fator de conversão armazenado.

--------------------------------------------------------------------------------
5. ONDAS DE EXECUÇÃO
--------------------------------------------------------------------------------
  ONDA 0 — Fundação ...................................... ✅ 2026-09-01
    Migrations (6 entidades + catálogo) + models + RecalcularGeoMetadata +
    permissões (seeder, Policies, CAIXA RoleResource, whitelist) + grupo de
    navegação "Mobilidade Urbana" (registrar em AppPanelProvider::
    navigationGroups — grupo fora da lista vai pro fim!) +
    CampoCustomizado::ENTIDADES + kit Piúma + seed do catálogo de sinalização.
    ✔ Entregue: migration 2026_09_01_200000 (7 tabelas, geo+GIST+GIN+índice
      único parcial), 7 models (azimute calculado + inverterDirecao no
      MobTrecho), 7 Policies (temPermissao), 13 permissões novas no
      PermissionsSeeder, CAIXA 19c + camadas na CAIXA 20, MobilidadeSeeder
      (39 tipos de sinalização + 23 campos de kit). Smoke test local:
      azimute 45°→225° no inverter, 148,8 m, MultiPolygon promovido, FK
      restrict do catálogo ok.

  ONDA 1 — Importação .................................... ✅ 2026-09-01
    Entidades mob_* no seletor do "Importar Mapa (GIS)" + ENTIDADE_CUSTOM;
    de/para sinalização name→tipo do catálogo; clip das rodovias;
    dedup dos fluxos; tenant Piúma (imobiliario + mob_infra) + bairros +
    gis:recalcular-metadata.
    ✔ Entregue: comando `mob:normalizar-levantamento <pasta-crua> --saida=`
      (JSONs crus → 6 GeoJSONs normalizados + bairros + relatorio.txt) e
      ramo Mob* no ImportadorGisService (mapeamento por camada + resolver
      nome→id do catálogo de sinalização, fallback "A Classificar").
    ✔ Carga local completa no tenant prefeitura-de-piuma:
      17 bairros · 612 trechos (112,4 km; extensão+azimute+26 atributos 100%)
      · 832 sinalizações (TODAS ligadas ao catálogo; só 4 "A Classificar" —
      99,5% de classificação automática) · 184 POIs · 75 eixos (29 rodovias
      recortadas de 896 do ES; 22 com extras DER-ES) · 102 zonas · 62 fluxos
      (1 duplicata exata removida). Zero vínculos não resolvidos.
    ✔ VPS (quando for a hora): criar a prefeitura no /admin (módulos
      imobiliario + mob_infra) → PermissionsSeeder + permission:cache-reset →
      MOB_SEED_TENANT=<slug> db:seed MobilidadeSeeder → subir os JSONs da
      pasta normalizada pela ação "Importar Mapa (GIS)" (Bairro primeiro) →
      "Recalcular Áreas (GIS)".

  ONDA 2 — Mapa .......................................... ✅ 2026-09-01
    Cases no MapDataController + layerConfigs + acordeon "Mobilidade Urbana"
    (um toggle por camada, gates ver_camada_mob_*); ficha ao clicar;
    criar/editar geometria/dados/excluir via traits; SETAS de sentido +
    Inverter (§3); TEMATIZAÇÃO dos trechos: select "Colorir por".
    ✔ Backend: 6 cases self-contained no MapDataController (trechos levam
      dados_customizados como `custom` p/ tematização; gzip já cobre).
      Permissões entram SOZINHAS (o endpoint map-permissions deriva de
      ver_camada_* dinamicamente).
    ✔ Engine: 6 layerConfigs (setas de mão única rotacionadas por segmento;
      tracejado = sentido não classificado; sinalização = círculo V /
      losango H na cor do catálogo; fluxos com espessura ∝ valores; zonas
      por tipo; eixos por tipo), clickPriority/hover/cirurgia em loop,
      "Colorir por" com legenda automática (colunas + campos do kit),
      mini-checkboxes de filtro por tipo/categoria (.mob-sub-toggle),
      criação (enableDrawing/Mesa de Desenho/Azimutes) e edição de
      geometria registradas p/ as 6 entidades.
    ✔ Painel: acordeon "Mobilidade Urbana" (gated por módulo) + grupo
      "Mobilidade Urbana" no dropdown Criar Artefatos; MapaFullscreen com
      os 6 traits HasMob*Actions (modais criar/editar com Inverter Sentido
      no trecho, catálogo na sinalização, campos do kit em trecho/eixo).
    ✔ Smoke: 6 camadas servidas (612/832/184/75/102/56 features), views
      compiladas, traits carregados sem colisão.
    ℹ️ ACHADO: 6 fluxos O/D são INTRAZONAIS (origem=destino, ex.: Centro→
      Centro com 87 viagens) — vieram com geometria VAZIA do levantamento;
      ficam no banco (telas/exports da Onda 3) e fora do mapa por natureza.

  ONDA 3 — Filament ...................................... ✅ 2026-09-01
    ✔ Grupo "Mobilidade Urbana" com 7 Resources CRUD-modal: MobTrecho
      (vocabulários em MobTrecho::VOCABULARIOS — fonte única com o mapa;
      filtros sentido/tipologia/pavimentação/estado; azimute na tabela),
      MobSinalizacao (badge na COR do catálogo; filtros por tipo e V/H;
      texto original da coleta preservado), MobTipoSinalizacao (catálogo:
      cor + ícone + CTB + contagem de placas; delete de tipo em uso é
      BLOQUEADO — orienta desativar), MobPontoInteresse, MobEixo (extensão
      em km; extras DER-ES via kit), MobZona (campos condicionais por
      tipo), MobFluxo (badge "Intrazonal" p/ os 6 sem linha).
    ✔ Exports XLS/PDF/CSV/XML nos 6 (MobilidadeExportService PARAMETRIZADO
      por entidade — desvio consciente do 1-service-por-entidade; Excel/CSV
      incluem os campos do kit via colunasExport; PDF na blade
      pdf.default-report; ActionGroup via trait TemExportacaoMobilidade
      respeitando os filtros da tabela).
    ✔ Tenant ganhou os 7 hasMany (exigência do $tenantRelationshipName).
    ✔ Smoke: 6 entidades × 4 formatos sem erro; 7 Resources carregam.

  ONDA 4 — Dados / pós-importação ........................ ✅ 2026-09-01
    ✔ CANETA DE SENTIDO: Ferramentas → "Classificar Sentidos (Trechos)" —
      painel flutuante com 3 canetas (Mão única / Mão dupla / Inverter→mão
      única) + contador "sem sentido" ao vivo; com a caneta armada, cada
      clique num trecho aplica na hora (tracejado vira sólido+setas).
      Backend: HasMobTrechoActions::aplicarSentidoTrecho (teste: inverter
      = +180,0° exatos no azimute).
    ✔ Revisão da sinalização "A Classificar": filtro por tipo no
      MobSinalizacaoResource + reclassificação pela tela/catálogo.
    ⏳ OPERACIONAL (com a equipe de Piúma): classificar os 612 trechos com
      o desenho original do lado (ou inferir pelas setas do projeto, se P2
      vingar) e revisar os 4 pontos "A Classificar".

  AFINAMENTOS pós-teste do usuário ....................... ✅ 2026-09-02
    ✔ Modais dos 6 traits do mapa: xl/lg → 3xl (botões do rodapé numa linha).
    ✔ Acordeon: sub-itens nascem DESLIGADOS; ligar a camada-mãe marca todos
      (cascata via listener .layer-toggle mob_* no engine), desligar desmarca;
      separador discreto entre os blocos; swatch das Zonas deixou de parecer
      um checkbox (era borda + fundo translúcido).
    ✔ "Classificar Sentidos" virou BARRA discreta no rodapé, centralizada,
      com x-teleport pro body (fixed quebrava dentro de ancestral com
      transform — era o layout esticado do print do usuário).
      ⚠️ 2026-09-03: a barra "não abria" — na verdade abria FORA da tela:
      bottom-5/left-1/2/bg-sky-50 etc. não existem no CSS pré-compilado do
      Filament (painel sem viteTheme). Corrigido com style inline (padrão
      da pílula de numeração). Reproduzido/validado headless (playwright-core
      + Chrome do sistema): barra centralizada, caneta arma, cursor crosshair.
    ✔ SETAS mais visíveis (2026-09-03, pedido do usuário): ponta de seta
      branca c/ contorno escuro, alongada, repetida a cada 70 px ao longo da
      linha inteira, raio cresce com o zoom (8→12). Antes: triângulo raio 7
      na cor da linha, 1 por segmento — sumia dentro da linha.
    ✔ SIMULADOR DE FLUXO (2026-09-03): checkbox "▶ Simular fluxo (animar
      setas)" no painel de camadas (abaixo do Colorir por) — as setas das vias
      de mão única andam no sentido da via (rAF ~30 fps, 45 px/s, re-render só
      da camada mob_trechos). Só visual. Validado headless: fase avança
      (18→45 px em 0,5 s), sem erro de console. Ajustes finos (velocidade,
      espaçamento, cor) = constantes MOB_SETA_PASSO / MOB_FLUXO_VEL no engine.

  ONDA 5 — MONITORAMENTO EM TEMPO REAL (câmeras) — 2026-09-03 ✅
    Pedido do usuário (fora do escopo original, aprovado): camada "Monitoramento
    em tempo Real" (checkbox abaixo do "Simular fluxo") com ícones de câmera;
    clique abre o player ao vivo. DEMO REAL: Piúma já publica 7 câmeras
    municipais (IZ1 Telecom, player FullCam, embed público sem restrição de
    frame) no site da prefeitura — seed MobCamerasPiumaSeeder.
    ✔ Entidade MobCamera (mob_cameras: POINT + nome/tipo[embed|youtube|hls|
      imagem]/url/url_snapshot/provedor/azimute_visada/ativo), padrão mob
      completo (tenant, sequential, soft delete, auditoria, geo log, Policy
      gerenciar_mob_cameras + ver_camada_mob_cameras, CAIXA 19c/20, Resource
      CRUD-modal "Monitoramento em Tempo Real" c/ export XLS/PDF/CSV/XML).
    ✔ Mapa: camada mob_cameras (ícone SVG de câmera, cone de visada pelo
      azimute, hover "Ao vivo — clique p/ assistir"), criar pelo "Criar
      Artefatos → Câmera de Monitoramento", modal = PLAYER em cima (iframe /
      YouTube embed / HLS c/ hls.js / snapshot JPEG a cada 5 s) + dados em
      seção recolhida + Reposicionar/Excluir. Vídeo só carrega no clique.
    ✔ Validado headless (2026-09-03): Resource lista as 7; camada carrega 7
      feições (cone + ícone); modal abre com player 848×477, badge AO VIVO,
      seção Dados, Reposicionar/Excluir. O embed FullCam toca em contexto
      SEGURO (top-level e iframe sob página HTTPS: vídeo 1920×1080 playing);
      sob o Laragon HTTP local o iframe fica em Loading (crypto.randomUUID
      indisponível em ancestral inseguro) — na VPS (HTTPS) não ocorre.
      Posições das 7 são aproximadas (POIs/orla/ES-060×ES-375): ajustar com
      Reposicionar junto com a prefeitura.
    ✔ "Vídeo piscando" (relato do usuário, 2026-09-03): era o app da FullCam
      em loop de recarga sob página HTTP (crypto.randomUUID só em contexto
      seguro; 25 recargas/12 s medidas; 0 requests Livewire; iframe nosso
      intacto). Player agora só monta o iframe em isSecureContext — senão
      aviso + "Abrir em nova aba" + "tentar mesmo assim"; box com wire:ignore
      (troca de tipo no select não recria o player). Em HTTPS (VPS) toca.
    ℹ️ Gotcha do painel de camadas: todo .layer-toggle precisa de
      <span class="layer-label"><span class="layer-text"> após o input —
      o fetchAndDrawLayer escreve "Carregando..." nele e quebra sem isso.
    ⏳ Câmeras RTSP da prefeitura (Intelbras/Hikvision): precisam de servidor
      de mídia (MediaMTX → HLS/WebRTC) numa caixa na rede da prefeitura
      publicando p/ a VPS — fora deste passo; o tipo "hls" já está pronto.
  ONDA 6 — VIAS URBANAS (fluxo) × TRECHOS (levantamento) — concluída 2026-09-03 ✅
    Reunião com a Dani (mobilidade): a DIREÇÃO do trecho (início→fim) é a
    direção em que o coletor andou = define calçada DIREITA/ESQUERDA dos 26
    atributos. Trecho NUNCA tem sentido de tráfego e sua geometria NUNCA pode
    ser invertida (o ST_Reverse do "Inverter Sentido" trocaria as calçadas).
    O fluxo pertence às VIAS. Decisão do usuário: tabela própria `mob_vias`
    (em Piúma 1:1 com os trechos; em outro município a via pode ser a soma de
    vários trechos — por isso NÃO é atributo do trecho).
    ✔ Modelo: MobVia (mob_vias, MULTILINESTRING): nome nullable, sentido
      [mao_unica|mao_dupla|null], azimute CALCULADO (direção = ordem dos
      vértices; ST_Reverse permitido — é a geometria da via, não do trecho),
      extensao_geo, logradouro_id nullable, dados_customizados. A FK fica no
      TRECHO: `mob_trechos.via_id` (nullOnDelete) — decisão do usuário ("o
      certo é o inverso": via não guarda trecho de origem; N trechos → 1 via).
      A coluna `sentido` SAIU de mob_trechos (`azimute` fica = direção do
      mapeamento). Migration 2026_09_03_200000_create_mob_vias_table.
    ✔ Permissões PRÓPRIAS gerenciar_mob_vias + ver_camada_mob_vias (CAIXA
      19c/20, EditRole, PermissionsSeeder, MobViaPolicy). Resource "Vias
      Urbanas" (nome, sentido, logradouro, nº de trechos; filtro por sentido)
      + export XLS/PDF/CSV/XML (spec mob_via). Trecho ganhou coluna/Select
      "Via urbana" (busca por nome ou número).
    ✔ Mapa: camada "Vias Urbanas (sentido)" no acordeon, visual PlanMob —
      azul #2563eb mão única (setas brancas no sentido do fluxo), vermelho
      #dc2626 mão dupla (setas nas DUAS pontas, ⬌), cinza tracejado sem
      classificação. Caneta "Classificar Sentidos (Vias Urbanas)", "Inverter
      Sentido" do modal, setas animadas e "Simular fluxo" MIGRARAM do trecho
      para a via (mão dupla anima nos dois sentidos). Trechos Viários: mantém
      atributos/"Colorir por"; tique DISCRETO cinza/branco sempre visível a
      partir do zoom 16 = direção do mapeamento; modal sem "Inverter",
      caneta e simulador não pegam trecho; ficha mostra a via vinculada.
      Clique com as duas camadas ligadas abre a VIA (prioridade de clique);
      HasMobViaActions no MapaFullscreen; "Criar Artefatos → Via Urbana".
    ✔ Dados: `php artisan mob:gerar-vias-dos-trechos --tenant=<slug>
      [--sentido=mao_dupla|mao_unica|nenhum]` cria 1 via por trecho vivo
      (MESMA geometria e MESMO sequential_id — o JSON do Pedro, por id, casa
      com as duas tabelas), vincula via_id e calcula extensão/azimute;
      idempotente (via existente só vincula). Rodado local: 612 vias, todas
      mão dupla (decisão: não esperar o Pedro; a equipe da mobilidade corrige
      no mapa com a caneta). Normalizador: mob_trechos.json agora sai SEM
      sentido e COM via_id (= id) + mob_vias.json (mesma geometria; sentido
      do JSON cru quando mao_unica|mao_dupla, senão mao_dupla). Importar Mapa
      GIS: camada "Mobilidade — Vias Urbanas" (importar ANTES dos trechos;
      o trecho resolve via_id pelo número da via) — trechos em ATUALIZAR.
      VPS: tenant de teste, será recarregado do local (não há migração de
      dados remota a fazer). gis:recalcular-metadata cobre mob_vias.
    ✔ 2026-09-04 — trechos.json do Pedro chegou (C:\laragon\www\docs\jsons-
      enviados\trechos.json): mesmos 612 ids, geometria e atributos IDÊNTICOS
      ao levantamento original + coluna `sentido` = 575 mao_dupla / 37
      mao_unica. Normalizador rodado em cima dele: C:\laragon\www\docs\
      piuma-normalizado\mob_trechos.json (regravado: só saiu `sentido`,
      entrou `via_id`) + mob_vias.json (novo). Importador GIS agora
      RECALCULA extensão/azimute (trechos, vias), extensão (eixos) e área
      (zonas) ao final de cada importação mob_* (recalcularMetadataMob) —
      não depende mais do "Recalcular Áreas" (cujo Select também ganhou as
      4 entidades mob). Ensaio local do roteiro da VPS (service, modo
      atualizar): vias 612 atualizadas, trechos 612 atualizadas + via_id
      resolvido pelo número, sentidos 575/37, metadados 100% preenchidos.
    ⚠️ Deploy/VPS (base que JÁ tem os 612 trechos): migrate +
      PermissionsSeeder + permission:cache-reset; depois, no /admin →
      Importar Mapa (GIS): 1) "Vias Urbanas" em ADICIONAR com mob_vias.json;
      2) "Trechos Viários" em ATUALIZAR com mob_trechos.json (grava via_id).
      Alternativa sem JSON de sentido: mob:gerar-vias-dos-trechos (1:1, tudo
      mão dupla) e depois mob_vias.json em ATUALIZAR só para aplicar o
      sentido.

  AJUSTES PRÉ-REUNIÃO DE 08/09 (pedidos da Dani, 2026-09-04) ✅
    ✔ Observação no trecho: coluna text `mob_trechos.observacao` (migration
      2026_09_04_100000); textarea no modal do mapa e na tela; coluna
      (toggleable, busca) e export; importável pela property `observacao`;
      aparece no hover do trecho (📝, 90 chars).
    ✔ Setor censitário IBGE: cor nova (âmbar #b45309, fill 14%) — era cinza
      quase transparente.
    ✔ DEMOGRAFIA por setor (mesmo dia, à tarde): a Dani mandou "Densidade
      Demográfica.json" (C:\laragon\www\docs\jsons-enviados\piuma) — JSON do
      ArcGIS (Esri: features[].attributes + geometry.rings, SIRGAS 2000),
      86 setores, CD_SETOR casa 100% com os códigos já importados, polígonos
      idênticos (Hausdorff ~7e-7°). Campos: V002 = população (soma 22.300),
      Hab_Km2 = densidade em hab/HECTARE (nome engana; bate com a legenda do
      PDF deles, máx 144,09), Hect = área, Renda (85/86). Implementado:
      colunas `populacao`/`densidade`/`renda` em mob_zonas (migration
      2026_09_04_110000); normalizador lê o arquivo (lerDemografia, aceita
      Esri ou GeoJSON) e casa por código nos setores do mob_zonas.json
      (regravado em piuma-normalizado; demais JSONs conferidos idênticos);
      importador mapeia os 3 campos; hover/ficha/tabela/export; e o mapa
      COROPLÉTICO "Colorir setores IBGE por…" (população/densidade/renda,
      6 classes por quantil, legenda com faixas, rótulo do valor no zoom ≥ 15;
      MobZona::TEMAS_SETOR → window.mapConfig.mobSetorTemas; engine evento
      sigweb-mob-setor-tema, paleta amarelo→marrom própria). Ensaio local:
      mob_zonas.json em ATUALIZAR = 102 zonas atualizadas, 86 setores com
      população. VPS: "Zonas de Estudo" em ATUALIZAR com o mob_zonas.json.
    ✔ Fluxos O/D: quantidades absolutas REMOVIDAS do mapa (pedido do
      usuário): ficha, hover, legenda e title dos checkboxes só com
      percentuais; o volume continua na tela "Fluxos O/D" do menu.
    ✔ Fluxos O/D SÓ LEITURA no mapa (decisão: só no mapa; a tela "Fluxos O/D"
      do menu continua editável): saiu o "Fluxo O/D (Linha)" do Criar
      Artefatos, o modal virou ficha (destino, volume, % do total geral, %
      dentro do destino) sem salvar/geometria/excluir; HasMobFluxoActions
      ficou só com a ficha. COR por região de destino (MobFluxo::CORES —
      centro vermelho, norte azul, sul verde, leste âmbar, nordeste roxo,
      sudoeste ciano, oeste rosa; fonte única p/ engine via
      window.mapConfig.mobFluxoCores e p/ legenda) + mini-checkboxes por
      destino (mob-sub-toggle, mesmo mecanismo das zonas: "só o que vai
      para o Centro") com o % do destino ao lado. RÓTULO da linha = % do
      TOTAL GERAL (decisão do usuário; MapDataController manda `percentual`
      e `total`; o total inclui os 6 intrazonais — MULTILINESTRING EMPTY,
      não NULL: 159 deslocamentos = 23% do total, sem linha no mapa; a
      legenda avisa isso p/ os rótulos visíveis somarem ~77% e ninguém
      estranhar). Espessura segue ∝ volume; linhas com valor 0 (27) ficam
      sem rótulo.
    ✔ ORIGEM × DESTINO — SEMÂNTICA CORRIGIDA (2026-09-04, tarde; usuário
      confirmou): cruzando as pontas das linhas com as zonas O/D no PostGIS,
      TODAS as linhas de um grupo `fluxo` (ex.: "Centro") SAEM do mesmo ponto,
      dentro da zona com aquele nome, e terminam cada uma numa zona diferente.
      Logo `fluxo` = ORIGEM (ponta compartilhada), não destino. Provas: 251
      viagens Nordeste → Central (e não o contrário); soma pelas pontas finais
      = Central 79,5%, Sudoeste 10,9%, Leste 3,5%… na mesma ordem do
      `destinos` do zonas.json (Central 68,62%). O grupo "Sudoeste" (16 linhas)
      são na verdade 2 origens (Oeste + Sudoeste) — a derivação separa.
      Implementado: migration 2026_09_04_120000 renomeia `mob_fluxos.destino`
      → `origem_regiao` (rótulo do levantamento) e cria `origem_zona` /
      `destino_zona` DERIVADAS (zona O/D que contém a ponta inicial / final;
      sem contenção = mais próxima; intrazonais herdam a zona mais frequente
      do grupo como origem E destino) — MobFluxo::recalcularOrigensDestinos(),
      chamado pelo importador ao importar fluxos OU zonas e pelo comando
      `mob:fluxos-origem-destino --tenant=<slug>`. Cores/checkboxes/% agora
      por DESTINO REAL (MobFluxo::distribuicao: paleta por ordem de volume,
      cor viaja na feature; sem CORES fixas por nome). Ficha: "Nordeste →
      Central" + origem/destino/percentuais. Tela Fluxos O/D: colunas Origem
      e Destino (derivadas, só leitura) + filtros; export idem. Normalizador
      grava `origem_regiao` (era `destino`) e `fonte` (era `origem`); o
      importador aceita o JSON antigo. mob_fluxos.json regravado.
      VPS: migrate + `mob:fluxos-origem-destino` (ou a importação das zonas
      em Atualizar, que também recalcula).
    ⚠️ Para o Pedro: nas ORIGENS, o zonas.json diz Leste 36,89% / Nordeste
      6,71%, mas as linhas dizem Nordeste 41,7% / Leste 7,7% — parece que a
      tabela do zonas.json trocou Leste↔Nordeste (os polígonos e o fluxos.json
      concordam entre si). Não afeta o mapa; afeta só a coluna `origens`.

    ℹ️ 2026-09-03 trechos.json da Líder com xi/yi/xf/yf (UTM 24S): são o 1º e
      o último vértice da linha — 611/612 batem com a ordem já importada
      (ids 274 e 321 com o par copiado do vizinho). DIREÇÃO já estava certa;
      o que falta continua sendo o SENTIDO (mão única/dupla) — usuário vai
      pedir coluna de sentido à Líder; se vier, aplicar por sequential_id.
    ✔ HOVER nas 6 camadas: destaque visual (halo/anel/engrossamento) +
      tooltip (trecho: sentido+pavimentação+extensão; eixo: km; fluxo:
      volume; placa: V/H; zona: código IBGE; POI: categoria).
    ✔ COORDENADA com copiar (pedido 2026-09-02): trait TemCoordenadaMob —
      coluna "Coordenada" copyable nas 6 tabelas + campo com botão 📋 nos
      modais de edição; ponto = posição, linha/zona = ST_PointOnSurface
      (ponto central SOBRE a geometria); formato "lat, lon" = o mesmo do
      "ir para coordenada" do mapa; guard p/ geo nula/vazia (intrazonais).
    ℹ️ JSONs normalizados copiados p/ C:\laragon\www\docs\piuma-normalizado
      (fora do repo/scratchpad) — é a pasta a usar no carregamento da VPS.

  CEREJA DO BOLO (fora de escopo agora — decisão P6) ................ 🍒
    Campanha/histórico de recadastro dos trechos (molde coleta_imobiliaria).
    + Reunião com a gestora Dani (2026-09-03) deve trazer novos ajustes.

--------------------------------------------------------------------------------
6. QUALIDADE DOS DADOS — decisões aplicadas
--------------------------------------------------------------------------------
  6.1 ✅ DECIDIDO: sinalização vira CATÁLOGO (tipo pré-definido com name, tipo
      V/H, cor e ícone) + pontos que só carregam posição — modelagem §2.1/2.2.
      O texto cru da coleta fica em descricao_original.
  6.2 ✅ DECIDIDO: rodovias só intersectando Piúma; banco em METROS (padrão
      extensao_geo), exibição em km — sem coluna de conversão.
  6.3 ✅ DECIDIDO: trecho sem name (referência = sequential_id); direção pela
      ordem do desenho; setas no mapa; campos sentido + azimute (§3).
  6.4 ✅ P4 DECIDIDO: fluxos ENTRAM; dedup por geometria idêntica na
      importação (1 duplicata exata removida — as 17 linhas de Sudoeste são
      geometrias DISTINTAS, levantamento legítimo em 2 lotes).
  6.5 ✅ Resolvido pelo usuário: JSON corrigido para `extensao_geo`.
  6.6 ✅ area_geo dos setores IBGE: ignorar valor do arquivo, recalcular PostGIS.
  6.7 ✅ Ciclovias/eixos: nome importa como veio, sem extração de situação.
  6.8 ✅ CRS84 em tudo — compatível direto (ST_GeomFromGeoJSON, SRID 4326).

--------------------------------------------------------------------------------
7. DECISÕES — situação
--------------------------------------------------------------------------------
  P1 ✅ Piúma ativa `imobiliario` + `mob_infra`.
  P2 ⏳ Setas visuais do projeto: usuário vai verificar com o geo (fica p/
       depois). Enquanto isso, sentido manual + azimute calculado (§3).
  P3 ✅ Rodovias: importar recortado.
  P4 ✅ MobFluxo entra, com dedup por geometria idêntica (ver 6.4).
  P5 ✅ Grupo "Mobilidade Urbana"; rótulos a critério do dev, refinamento depois.
  P6 ✅ Campanha/histórico = cereja do bolo, fora do escopo inicial.
================================================================================
